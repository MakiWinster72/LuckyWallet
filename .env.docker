# Database
DB_ROOT_PASSWORD=rootpass
DB_USER=luckywallet
DB_PASSWORD=luckywallet
DB_NAME=luckywallet

# JWT (change this!)
JWT_SECRET=replace-with-a-random-secret-at-least-32-characters
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend origin (for CORS)
FRONTEND_ORIGIN=http://localhost
